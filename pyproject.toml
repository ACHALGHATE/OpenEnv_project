[project]
name = "openenv-ai-assistant"
version = "1.0.0"
description = "Multi-task OpenEnv environment"
authors = [{name = "Achal"}]

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "app:app"

[tool.openenv]
env = "env:WorkEnv"